<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context="com.softmed.ccm_facility.activities.MainActivity">


    <item android:id="@+id/logout" android:title="@string/logout"
        android:orderInCategory="100" app:showAsAction="never" />

    <item android:id="@+id/sync_data"
        android:title="@string/sync"
        android:orderInCategory="100"
        app:showAsAction="never"
        android:icon="@mipmap/ic_action_sync"/>

    <item android:id="@+id/reports"
        android:title="Reports"
        android:orderInCategory="100"
        app:showAsAction="always"
        android:icon="@mipmap/ic_action_reports"/>

    <item android:id="@+id/reminder"
        android:title="Reminder"
        android:orderInCategory="100"
        app:showAsAction="always"
        android:icon="@mipmap/ic_reminder"/>

</menu>
